function letterCombinations(digits: string): string[] {
    let firstNum = Number(digits[0])

    const map = [
        'abc',
        'def',
        'ghi',
        'jkl',
        'mno',
        'pqrs',
        'tuv',
        'wxyz'

    ]


    function backTraking() {

    }

    for (let i = 0; i < map[9].length ) { }

};